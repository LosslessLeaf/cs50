#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    // string gender = get_string("What is your gender? (Male/Female) ");
    // string firstName = get_string("What is your first name? ");
    // string lastName = get_string("What is your last name? ");
    // if (strcmp(gender, "Male") == 0)
    // {
    //     printf("Hello Mr. %s %s!\n", firstName, lastName);
    // }
    // else {
    //     printf("Hello Ms. %s %s!\n", firstName, lastName);
    // }


    int myNum = 36;
    float myFloat = 3.87;
    float other = 1.52;
    float division = (float)83/(float)15;
    printf("adding the numbers: %.1f\n", myFloat + other);
    printf("the division: %.1f\n", division);
}